target/
.DS_Store
# docs/ lives in ~/docs/repo-docs/copad (private SSoT); this is a symlink, tracked there only
/docs
*.swp
*.swo
.claude/
nestty-macos/.build/
nestty-macos/DerivedData/
# Catch SwiftPM build dirs anywhere in the workspace (Phase 0 spike
# accidentally committed its .build/ because we only ignored the
# nestty-macos-specific path).
**/.build/
nestty-macos/*.xcodeproj/xcuserdata/
nestty-macos/*.xcworkspace/xcuserdata/
